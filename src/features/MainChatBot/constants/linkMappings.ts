// Used to detect links in the chatbot and display them as clickable links with a custom text
export const linkMappings: { [key: string]: string } = {
  "localhost:3000/course/": "Visit Course",
  "localhost:3000/problems/": "Visit Problem"
};
